using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace scripts.bob.interfaces
{
    interface ISlot
    {
        void Initialize(); // interface method (does not have a body)
    }
}
